**架构设计题答案：**

1.一个 session 连续聊了 200 轮，context 快爆了。你会怎么做压缩？如何确保压缩后的对话仍然流畅？

​	核心策略就四个字：摘要+遗忘。

​	具体是：当对话接近上下文限制时，把前面的历史交给模型做一次语义压缩，提炼出「已做的决策」「遗留问题」「用户偏好」「关键代码约定」形成结构化摘要放在上下文头部，同时丢弃中间的低价值轮次（如调试日志、打招呼、确认），只保留最近 N 轮完整对话放在尾部。这样模型回复时头部有全局状态、尾部有当前语境，中间不塞无用信息，就能保证压缩后对话依然连贯。实际工程中还可以用持久化记忆文件替代内存摘要，把跨 session 的知识固化到磁盘，进一步减少上下文占用。



2：和聊天 Agent 熟悉半个月后，用户问了一个以前问过的问题。Agent 如何做 memory 召回更合理？



​	最合理的做法是多路召回 + 重排序：先把用户当前问题做 embedding，对记忆库做语义相似度搜索召回Top-K；同时用规则（关键词匹配、同用户历史问题指纹匹配）做另一路召回；两路结果合并后，用一个轻量排序模型（或 LLM自己）按时效性 + 相关性 + 重要性综合重排，保留最匹配的 1-3条注入上下文。关键技巧是：时效性要打折——半个月前的记忆即使语义匹配，权重也要低于最近确认过的同类信息，避免把用户已改变的想法翻出来。同时要设计记忆置信度，用户如果纠正过一次，该条记忆自动降权。

3：对于长程任务，大模型执行一段时间可能会忘掉目标，你知道哪些解决方案，有什么优缺？



核心思路是显式化目标保持，我有几种方案：

    1. 系统提示注入（目标锚定）：每轮都在 System Prompt 开头重述原始目标任务，强制模型看到。优点是零开销，缺点是占 context
       且模型可能"看过但不在意"。
    2. 结构化进度追踪（Checklist）：把总目标拆成子任务列表，每完成一项就标记，失败就记录原因。优点是可视化进度、方便断点续
       跑，缺点是需要在回复格式中强制约束。
    3. 外部记忆回写（Memory/File）：每一步都让模型把「当前状态+已做决策+剩余目标」写入持久化文件，下一步先读文件再执行。优
       点是即使 context 被截断也不丢目标，缺点是写文件有 IO 开销。
    4. 子任务隔离（Sub-agent）：把长程任务拆成多个独立子步骤，每个子 Agent
       只接收当前步骤的目标，不关注全局。优点是不存在遗忘问题，缺点是需要编排逻辑，且子任务间信息传递可能遗漏。
    5. 反思/自查循环（Reflection）：每 N 步让模型回顾原始目标，对比当前进度，判断是否偏离。优点是能主动纠偏，缺点是增加
       token 消耗。

4：Agent 工具有同步和异步两类。异步工具不能让用户一直等，但结果依然重要。你会如何设计异步工具执行和完成通知？

异步执行 + 主动感知：Agent 提交任务后立刻返回 task_id给用户并继续干别的，不等待；任务完成后结果写入持久化结果槽，Agent在下次回复前自动扫描槽位，发现已完成的任务就自然纳入回复。用户无需手动查结果，Agent像"突然想起来"一样告诉你任务搞定了。



5：Claude Code 的工具输出方式和国内 GLM / 豆包等 OpenAI-compatible function calling 有什么不同？他们各自这样设计的优缺点是什么？

核心区别在执行模型的架构分层不同：

  	Claude Code 的工具是"操作系统级原语"——工具（Read/Write/Bash/Edit）Harness（运行时）直接管理和执行，模型输出的是工具调用意图，Harness 负责调度、权限控制、结果回传。模型只管"我想做什么"，不关心底层怎么执行。

  	OpenAI-compatible FC（GLM/豆包等）是"应用级合约"——模型输出一个 JSON tool_calls，开发者自己写代码接住这个 JSON、调API、把结果塞回 messages。模型只输出声明，不参与执行。

---
  各自优缺点：

  Claude Code 工具（Harness 驱动）
  - 开发者负担：低——Harness 帮你管了权限、并行、结果回传
  - 权限控制：内建——执行前弹窗问用户
  - 并行粒度：灵活——可以交叉思考 + 多工具混发
  - 工具能力：强——可以读写文件、跑 Shell、操作 IDE
  - 可移植性：差——绑定 Claude Code 生态
  - 透明度：高——思考过程对用户可见

  OpenAI FC（开发者驱动）
  - 开发者负担：高——你要自己写调度、错误处理、重试
  - 权限控制：无——你自己实现安全策略
  - 并行粒度：批处理——所有并行调用必须一次性提交
  - 工具能力：弱——本质只是结构化 API 调用
  - 可移植性：好——标准化 JSON Schema，换模型只需改 endpoint
  - 透明度：低——黑盒输出 JSON，用户看不全推理链

  	一句话总结：Claude Code 适合深度 Agent场景（操作文件、跑代码、长任务编排），给你「模型+执行」一体的沉浸体验，但锁平台；OpenAI FC 适合轻量集成（查天气、发邮件、搜数据库），给你最大的灵活性和可移植性，但执行层得自己造轮子。